# Handoff to a Separate Project

## Current clarification, 2026-09-07

The product is named Product Development Workflow; the active skill and invocation are
`product-development-workflow` / `$product-development-workflow`. The historical baseline
and GitHub repository slug retain their previous names. The installed global copy was not changed.
Current decisions and the next step are in [SPEC.md](SPEC.md) §§6–8 and [PROJECT_STATUS.md](PROJECT_STATUS.md).
The bootstrap assignment below is historical: do not repeat completed modules or restore the
cancelled approval scheme or separate user-owned worker tasks.

This assignment is for the main coordinator of the created `WorkKroG/product-development-harness`
project. The owner authorized the transfer and plan preparation. This is not blanket approval of
the remaining proposals in SPEC.md.

## Transfer package

The core documents and baseline were transferred to the permanent project. The exact source
package with private evidence exists only in the ignored `.local-handoff/`. Public SOURCES.md
contains anonymized provenance. Preserve the source skill SHA. Do not install the baseline snapshot
as a parallel active skill or turn its historical instructions into authority for the new project.

## Initial coordinator assignment

Develop a harness for creating and continuing products in Codex based on the existing
product-development-cycle. The owner explicitly chose to evolve this skill in a separate,
reusable Git repository. The first supported environment is Codex desktop.

First read [README.md](../../README.md), [SOURCES.md](SOURCES.md), [AUDIT.md](AUDIT.md), and
[SPEC.md](SPEC.md). Reconcile the seven source files in `../../baseline/product-development-cycle`
against [BASELINE.sha256](../../BASELINE.sha256) and read them. Then use [EVALUATION.md](EVALUATION.md)
as the criteria for future evaluation. The new harness has not yet been implemented or piloted.

Verify the two bootstrap commits: the exact baseline, followed by the specification and project
instructions. Do not import fabricated history or change the installed global copy. The user
accepted the direction and authorized plan preparation; present the remaining material proposals
together with the first-working-version plan in this main chat. Do not ask again about the already
selected Codex-only scope or evolution of product-development-cycle.

The principle in SPEC.md §§5.1–5.4 is also agreed: design with future loads in mind, implement the
architecture in stages, and reassess its path before scaling. Preserve the five stages and the
connection among architecture vision, current solution, and evolution plan. Do not turn distant
hypotheses into mandatory infrastructure for the first prototype. At transition, use actual evidence
and ordinary PLAN review; allow the initial architecture path to change, with an explicit owner
decision for material consequences. The first prototype’s code may be discarded entirely when
validation justifies replacement. Do not require a rewrite or complicate the prototype for hypothetical
reuse. Preserve suitable parts, experiment results, and required data.

The owner also requested a light market, competition, and economics assessment between Positioning
and Journey: SPEC.md §5.5. Replace the former early Gate 4.5 with 3.5 in all active instructions and
checks. Reuse existing Positioning analysis; do not require a detailed financial model early. Gate 8
refines the same estimate for the next investment. Recognize suitable old 4.5 material without repeated
research; do not change the baseline.

After design approval, prepare a detailed plan of small, independently reviewable changes. Authorized
coordination uses user-owned Product/Task coordinators and internal Implementation, independent Change
Review, and PLAN/FINAL subagents. Assign models through native fields according to the accepted profile
and actual availability. Creating new user-owned tasks requires a mandate in the selected project.
Existing held tasks migrate through a current reviewed plan while preserving WIP/evidence; changing
the executor must not be used to bypass a denial.

Local plans, product decisions, and acceptance are agreed with the owner in the Task chat. Only major
events and project-wide escalations under SPEC.md §7 go to the main coordinator. When needed, it
organizes a separate architecture task with subagents, records the accepted decision, and returns new
continuation boundaries to affected tasks. Events travel through native messages; the owner does not
relay them manually. Manual merge remains with the owner. Platform restrictions apply independently
of skill text.

Do not take over development of the previous product or reuse its task IDs, Issues/PRs, or private
requirements as configuration for the new product. Local evidence is historical material used only
to derive rules; active templates are anonymized.

Keep third-party PM/UX/security/finance skills as explicit dependencies, with discovery in the
specific environment and a clear fallback. Do not install them silently.

Completing v0.1 requires independent review, meaningful evaluation, a test installation/update, and
a pilot. Green lint does not prove process quality, and a rehearsal is not a production release.

## Remaining decisions

Review of the first-working-version plan and material design proposals; distribution terms for source
materials before public push; and the selected pilot and its side effects. The repository name,
directory, and public visibility are already known; do not ask for them again.

## Handoff checks

- The new coordinator can read the documents without knowing the original conversation.
- It knows the mandate boundaries, skill source, and unvalidated proposals.
- The accepted specification version is identified by link/content.
- The first delivery plan does not begin by recreating the entire lifecycle from scratch.
- Old project context is not pulled in as context for the new product.
